001C80     ___frameset0:
001C80     DDE3          ex (sp),ix
001C82     ED2200        lea hl,ix
001C85     DD21000000    ld ix,$000000
001C8A     DD39          add ix,sp
001C8C     E9            jp (hl)

; stack
; hl
; ret
; ret <= ix point to last ret = ix <= value of hl






; stack : 
hl
ret

thread_entry:
	call	___frameset0
	ld	hl, (ix+6)
	
	
	
	
	pop	ix
	ret

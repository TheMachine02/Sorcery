Dynamic Linker library

>> load library in memory and load realloc table
>> realloc call in program based of the library
>> we need to export the function name for linking and compilation

int main ()
